Redirect 302 /test-htaccess /index.html

RewriteEngine On
RewriteBase /

# Only rewrite if the request is NOT for a real file or folder
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

